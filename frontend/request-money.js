document.querySelector('#requestForm').addEventListener('submit',e=>{e.preventDefault();const person=document.querySelector('#person').value.trim();const amount=Number(document.querySelector('#amount').value);const requests=JSON.parse(localStorage.getItem('torba:requests')||'[]');requests.unshift({id:'REQ-'+Date.now(),person,amount,dueDate:document.querySelector('#dueDate').value,note:document.querySelector('#note').value.trim(),status:'pending',date:new Date().toISOString()});localStorage.setItem('torba:requests',JSON.stringify(requests));document.querySelector('#message').textContent='Ödeme talebi oluşturuldu.';e.target.reset()})