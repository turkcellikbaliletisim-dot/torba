const loginForm=document.querySelector('#loginForm');const otpForm=document.querySelector('#otpForm');const message=document.querySelector('#message');loginForm.addEventListener('submit',e=>{e.preventDefault();const phone=document.querySelector('#phone').value.trim();if(phone.length<10){message.textContent='Geçerli bir telefon numarası girin.';return}sessionStorage.setItem('torba:phone',phone);loginForm.hidden=true;otpForm.hidden=false;message.textContent='Doğrulama kodu gönderildi.'});otpForm.addEventListener('submit',e=>{e.preventDefault();const otp=document.querySelector('#otp').value.trim();if(!/^\d{6}$/.test(otp)){message.textContent='Kod 6 haneli olmalıdır.';return}sessionStorage.setItem('torba:authenticated','true');location.href='dashboard.html'})