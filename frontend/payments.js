const payments=[{name:'QR Ödeme #1024',amount:'₺2.450',status:'Başarılı'},{name:'Kurumsal Ödeme #1023',amount:'₺8.900',status:'Başarılı'},{name:'İade #1022',amount:'₺780',status:'Bekliyor'}];const list=document.querySelector('#paymentList');const search=document.querySelector('#search');const status=document.querySelector('#status');function render(){const query=search.value.toLocaleLowerCase('tr-TR');const filtered=payments.filter(item=>item.name.toLocaleLowerCase('tr-TR').includes(query)&&(status.value==='all'||item.status===status.value));list.innerHTML=filtered.map(item=>`<article class="payment"><strong>${item.name}</strong><span>${item.amount}</span><span class="badge">${item.status}</span></article>`).join('')}search.addEventListener('input',render);status.addEventListener('change',render);render()