const movements=[{name:'Para yükleme',amount:'+ ₺5.000',type:'positive'},{name:'Market ödemesi',amount:'- ₺1.250',type:'negative'},{name:'İade',amount:'+ ₺780',type:'positive'}];document.querySelector('#walletList').innerHTML=movements.map(item=>`<div class="item"><span>${item.name}</span><strong class="${item.type}">${item.amount}</strong></div>`).join('');document.querySelector('#addMoney').addEventListener('click',()=>alert('Para yükleme ekranı sonraki sayfada açılacak.'))